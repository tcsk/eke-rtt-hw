const assert = require('chai').assert;

describe('Check dependencies', function(){
    it('Test should run without errors! If not, please check dependencies and versions!', function () {
        let actual = 'hello';
        let expected = 'hello';
        assert.equal(actual, expected);
    });
});
